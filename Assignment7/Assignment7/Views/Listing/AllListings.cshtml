
@{
    ViewData["Title"] = "AllListings";
}
@model IEnumerable<Listing>

@foreach (var l in Model)
{
    <div>
        <h3>@l.Name</h3>
        <p>@l.Description</p>
        <h4>@l.Price.ToString("c")</h4>
    </div>
}

